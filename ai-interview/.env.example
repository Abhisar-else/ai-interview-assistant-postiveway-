# Toggle mock data mode vs real FastAPI backend.
# IMPORTANT: if this file / VITE_USE_MOCK is missing entirely, the app
# defaults to USE_MOCK=true and silently serves canned mock data instead
# of calling the real backend. Always set this explicitly.
VITE_USE_MOCK=false
VITE_API_URL=http://localhost:8000/api
